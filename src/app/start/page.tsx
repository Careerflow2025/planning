import GuidedFlow from "@/components/GuidedFlow";

export const metadata = {
  title: "Start Your Report — PlanScope",
  description: "Enter your postcode and generate a UK planning report in minutes.",
};

export default function StartPage() {
  return <GuidedFlow />;
}
